@PACKAGE_INIT@

if(NOT TARGET mfftsaxs)
  include("${CMAKE_CURRENT_LIST_DIR}/mfftsaxsTargets.cmake")
endif()

check_required_components(@PROJECT_NAME@)

get_filename_component(MFFTSAXS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(SAMPLING_INCLUDE_DIRS "${SAMPLING_CMAKE_DIR}/../../../include/mfftsaxs")

set(SAMPLING_LIBRARIES mfftsaxs)
